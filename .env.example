# Supabase — copy from your project's Settings → API.
# These are public by design (the anon key is safe in the client; RLS is the
# security boundary). Put real values in .env.local (gitignored).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
